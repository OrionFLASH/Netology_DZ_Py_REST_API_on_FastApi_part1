# Домашнее задание к лекции «Создание REST API на FastApi» часть 2

Источник: репозиторий Netology — каталог `3.2-fast-api-2` (ветка `new`).

Ориентировочное время выполнения — 90 минут.

## Задание

Нужно доработать проект из предыдущего задания «Создание REST API на FastApi» часть 1:

1. Добавить роут `POST /login`. В теле запроса должен передаваться JSON с именем пользователя и паролем. Роут возвращает токен. В дальнейшем токен будет использоваться для авторизации. Срок действия токена — 48 часов. Если клиент предоставил неверный логин или пароль, сервис должен выдать ошибку 401.

2. Добавить роуты для управления пользователями:

   - `GET /user/{user_id}`
   - `POST /user`
   - `PATCH /user/{user_id}`
   - `DELETE /user/{user_id}`

   Пользователи должны принадлежать одной из следующих групп: `user`, `admin`.

3. Права неавторизованного пользователя (клиент может токен не передавать):

   - создание пользователя `POST /user`
   - получение пользователя по id `GET /user/{user_id}`
   - получение объявления по id `GET /advertisement/{advertisement_id}`
   - поиск объявления по полям `GET /advertisement?{query_string}`

4. Права авторизованного пользователя с группой `user`:

   - все права неавторизованного пользователя
   - обновление своих данных `PATCH /user/{user_id}`
   - удаление себя `DELETE /user/{user_id}`
   - создание объявления `POST /advertisement`
   - обновление своего объявления `PATCH /advertisement/{advertisement_id}`
   - удаление своего объявления `DELETE /advertisement/{advertisement_id}`

5. Права авторизованного пользователя с группой `admin`:

   - любые действия с любыми сущностями

Если у пользователя недостаточно прав для выполнения операции, возвращается ошибка **403**.

> В формулировке задания в пути встречается запись `GET /user/{user_id?}` — в реализации используется обязательный параметр `user_id`: `GET /user/{user_id}`.

---

## Ссылка на проверку решения

Сводная таблица эндпоинтов, заголовок `Authorization: Bearer`, переменные окружения и сценарии тестов — в корневом **`README.md`**.

Актуальные **результаты проверки** (прогон `pytest`, соответствие таблицам ТЗ, ручная проверка через Swagger, примечание по Docker) — в разделе **«Результаты последней проверки»** того же файла `README.md`.
