# Домашнее задание к лекции «Создание REST API на FastApi» часть 1

Источник: репозиторий Netology — каталог `3.1-fast-api-1` (ветка `new`).

## Задание

Нужно написать на FastAPI и докеризировать сервис объявлений купли/продажи.

У объявлений должны быть следующие поля:

- заголовок
- описание
- цена
- автор
- дата создания

Должны быть реализованы следующие методы:

- Создание: `POST /advertisement`
- Обновление: `PATCH /advertisement/{advertisement_id}`
- Удаление: `DELETE /advertisement/{advertisement_id}`
- Получение по id: `GET /advertisement/{advertisement_id}`
- Поиск по полям: `GET /advertisement?{query_string}`

Авторизацию и аутентификацию реализовывать **не нужно**.

---

## Ссылка на проверку решения

Таблица соответствия требованиям задания, коды ответов HTTP, параметры поиска и итоги прогона тестов приведены в корневом файле **`README.md`** проекта (разделы «Проверка соответствия заданию Netology» и «Результаты проверки работы»).
